manifestVersion: 1
id: ferntrail-bitgarth
category: finance
name: BitGarth
icon: https://bitgarth.app/assets/favicon/icon-512.png
version: "0.3.0"
tagline: Local-first crypto portfolio tracker
description: >-
  BitGarth is a local-first portfolio tracker for your own Bitcoin and
  Ethereum wallets. Create local users on your Umbrel — no central BitGarth
  account, email address, seed phrase, or private key is required.


  Add Bitcoin xpubs and Ethereum addresses for balance sync, track manual
  assets, see your net worth in your own currency, and export plain-text
  accounting files for hledger and ledger-cli.


  Your wallet data, labels, settings, and saved API keys stay in an encrypted
  local database protected by your password. BitGarth cannot unlock it for
  you, so keep a backup.


  An optional paid plan adds Bitcoin and Ethereum transaction-history backfill
  and reports to support tax filing. Upgrading does not create a central user
  account: BitGarth verifies payment and subscription identifiers that are
  separate from local app users with its entitlement service, while your
  portfolio and transaction data stay in your app instance.
developer: FernTrail B.V.
website: https://bitgarth.app
dependencies: []
repo: https://github.com/BitGarth/bitgarth
support: mailto:hello@bitgarth.app
port: 8390
gallery:
  - https://raw.githubusercontent.com/FernTrail/ferntrail-umbrel-app-store/refs/heads/main/ferntrail-bitgarth/gallery/1.jpg
  - https://raw.githubusercontent.com/FernTrail/ferntrail-umbrel-app-store/refs/heads/main/ferntrail-bitgarth/gallery/2.jpg
  - https://raw.githubusercontent.com/FernTrail/ferntrail-umbrel-app-store/refs/heads/main/ferntrail-bitgarth/gallery/3.jpg
  - https://raw.githubusercontent.com/FernTrail/ferntrail-umbrel-app-store/refs/heads/main/ferntrail-bitgarth/gallery/4.jpg
  - https://raw.githubusercontent.com/FernTrail/ferntrail-umbrel-app-store/refs/heads/main/ferntrail-bitgarth/gallery/5.jpg
path: ""
defaultUsername: ""
defaultPassword: ""
releaseNotes: |
  - BitGarth's source code is now published. Every release is built from a
    public commit you can read, audit, and build yourself.
  - A new `bitgarth` command-line client reads your wallet balances from a
    terminal. You pair it once in your browser. In this release it shows
    balances; later releases will give it more to do.
  - A Bitcoin account that reached its per-account transaction limit no longer
    shows "Syncing" forever.

  New:

  - The `bitgarth` command-line client prints a balance sheet of your wallets.
    Run `bitgarth pair`, open the approval link it prints, approve it in
    BitGarth, then run `bitgarth balancesheet` (short: `bs`).
  - Ready-to-run downloads of the CLI for Linux, macOS on Apple Silicon, and
    Windows are attached to each release, with checksums. You can also build it
    from source.
  - The CLI keeps a profile per BitGarth you connect to, so one machine can
    reach several installs. Manage them with `bitgarth profile list`,
    `bitgarth profile rename`, and `bitgarth profile remove`.
  - **Settings → Paired Clients** lists everything you have paired, with when it
    was created, when it expires, and when it was last used. Revoking one cuts
    off its access immediately, without affecting your password or your other
    paired clients.
  - The CLI requires HTTPS by default, and will not send its credentials over
    plain HTTP unless you allow that explicitly.

  Fixed:

  - A Bitcoin account that hit its per-account transaction limit stayed marked
    "Syncing" indefinitely and kept re-running sync in the background. It now
    settles once the permitted history has downloaded, and the history still
    waiting is picked up later if that limit is raised.
  - Refreshing your wallets or checking for a new version could break the page
    until you reloaded it.

  Notes:

  - BitGarth is published under the Functional Source License 1.1
    (FSL-1.1-ALv2). Each release additionally becomes available under the Apache
    License 2.0 on its second anniversary.
  - BitGarth's internal web addresses moved from `/api/v1/` to `/_app/`, and its
    health check is now `/health`. If you self-host behind a reverse proxy that
    filters or rewrites paths, allow `/_app/` and `/health`. Hosted, Umbrel, and
    plain Docker installs need no change.

  Full release notes at https://bitgarth.app/releases.html#0.3.0
submitter: FernTrail B.V.
